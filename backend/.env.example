DATABASE_URL="postgresql://ecosmok_admin:ApnaStrongPassword@localhost:5432/ecosmok"
PORT=4000
STOREFRONT_URL="http://localhost:3000"
OPENAI_API_KEY="sk-..."

# Used to sign login sessions for both admin and customer accounts.
JWT_SECRET="change-this-to-a-long-random-string"

# Optional: public base URL the API is reachable at, used to build
# /uploads/* image URLs returned by the upload endpoint. Defaults to
# http://localhost:<PORT> if not set.
API_PUBLIC_URL="http://localhost:4000"

NODE_ENV="development"
